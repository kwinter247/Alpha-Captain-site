# Copy to scripts/deploy.env (gitignored) and fill in.
# No passwords anywhere - auth is the SSH key ~/.ssh/alpha_medic_deploy
# (one key for the whole Hostinger Business plan; same SSH user covers
# every domain on it).
SSH_HOST=156.67.73.17
SSH_USER=u186382827
SSH_PORT=65002
REMOTE_ROOT=domains/alphacaptain.app/public_html
SITE_URL=https://alphacaptain.app
